const { statementScene } = require('./statementScene');
const { statement1Scene } = require('./statement1Scene');
const { statement2Scene } = require('./statement2Scene');
const { statement3Scene } = require('./statement3Scene');
const { statement4Scene } = require('./statement4Scene');
const { statement5Scene } = require('./statement5Scene');
const { statement6Scene } = require('./statement6Scene');
const { statement7Scene } = require('./statement7Scene');
const { statement8Scene } = require('./statement8Scene');
const { statement9Scene } = require('./statement9Scene');
const { statement10Scene } = require('./statement10Scene');

module.exports = {
  statementScene,
  statement1Scene,
  statement2Scene,
  statement3Scene,
  statement4Scene,
  statement5Scene,
  statement6Scene,
  statement7Scene,
  statement8Scene,
  statement9Scene,
  statement10Scene,
}